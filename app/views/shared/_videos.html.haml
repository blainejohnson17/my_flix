- videos.in_groups_of(6) do |video_group|
  .row-fluid
    - video_group.each do |video|
      - next if video.nil?
      .video.span2
        = link_to video do 
          %img(src="#{video.small_cover_url}")