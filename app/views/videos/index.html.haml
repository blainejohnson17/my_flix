%article.video_category
  - @categories.each do |category|
    %header
      %h3= link_to category.name, category 
    .videos.video_slider
      - category.recent_videos.each do |video|
        .video
          = link_to video do
            %img(data-lazy="#{video.small_cover_url}")